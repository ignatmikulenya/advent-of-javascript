import styled from "styled-components";

export const StyledSettingsButton = styled.button`
  border: none;
  background: none;

  cursor: pointer;
  opacity: 0.3;

  &:hover {
    opacity: 1;
  }
`;
